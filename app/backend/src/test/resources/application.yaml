spring:
  cloud:
    aws:
      secretsmanager:
        enabled: false
      region:
        static: ap-south-1
  datasource:
    url: jdbc:h2:mem:testdb;MODE=PostgreSQL;DATABASE_TO_UPPER=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
  mail:
    host: localhost
    port: 54324

supabase:
  url: http://localhost:54321
  anon-key: sb_publishable_ACJWlzQHlZjBrEguHvfOxg_3BJgxAaH
  jwt-secret: your-local-jwt-secret
  service-role-key: your-service-role-key-here

aws:
  s3:
    bucket: pec-events-assets-local
    region: ap-south-1
    endpoint: http://localhost:9000
  cdn:
    url: http://localhost:9000/pec-events-assets-local
